
import { LoginForm } from "./login-form"

export default function LoginPage() {
    return (
        <div className="flex items-center justify-center min-h-screen bg-[conic-gradient(at_top_left,_var(--tw-gradient-stops))] from-slate-100 via-pink-50 to-slate-100">
            <LoginForm />
        </div>
    )
}
